# Default Rules: Data

## Vault search

- Always search vault content when user asks about a topic — user expects indexed, searchable access to their information.
- Learn and understand vault content rather than just calling search functions. Comprehension over mechanics.
- After 3+ failed attempts on the same query: escalate strategy — check broader contexts, cross-reference related files, or explicitly admit the limitation rather than repeating the same approach.
